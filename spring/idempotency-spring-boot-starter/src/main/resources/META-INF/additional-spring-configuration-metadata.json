{
  "properties": [
    {
      "name": "idempotency.key-header",
      "type": "java.lang.String",
      "description": "HTTP header name used to carry the idempotency key.",
      "defaultValue": "Idempotency-Key"
    },
    {
      "name": "idempotency.default-ttl",
      "type": "java.time.Duration",
      "description": "Default time-to-live for stored idempotency records. After this duration, records become eligible for purging.",
      "defaultValue": "24h"
    },
    {
      "name": "idempotency.default-lease",
      "type": "java.time.Duration",
      "description": "Default duration an acquisition is protected before another caller may steal it. The heartbeat extends it at half this interval.",
      "defaultValue": "30s"
    },
    {
      "name": "idempotency.default-wait",
      "type": "java.time.Duration",
      "description": "Default duration a concurrent caller blocks waiting for an in-flight idempotency key before giving up. Zero means do not block.",
      "defaultValue": "10s"
    },
    {
      "name": "idempotency.filter-order",
      "type": "java.lang.Integer",
      "description": "Order of the idempotency servlet filter in the filter chain. Lower values execute earlier.",
      "defaultValue": 0
    },
    {
      "name": "idempotency.purge.enabled",
      "type": "java.lang.Boolean",
      "description": "Whether automatic purging of expired idempotency records is enabled.",
      "defaultValue": true
    },
    {
      "name": "idempotency.purge.cron",
      "type": "java.lang.String",
      "description": "Cron expression controlling how often expired idempotency records are purged.",
      "defaultValue": "0 0 * * * *"
    },
    {
      "name": "idempotency.max-body-bytes",
      "type": "java.lang.Long",
      "description": "Maximum allowed request body size in bytes for idempotency processing. Requests exceeding this limit receive HTTP 413. Set to -1 to disable the limit.",
      "defaultValue": 1048576
    }
  ]
}
